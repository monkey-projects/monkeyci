(ns monkey.ci.gui.test.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [monkey.ci.gui.core :as sut]))

#_(deftest failing
  (testing "fails for testing purposes"
    (is (= 1 0))))
