(ns monkey.ci.commands
  (:require [clojure.tools.logging :as log]))

(defn run-build-local [opts]
  ;; TODO
  (log/info "Running build locally:" opts))

(defn verify-build [opts]
  ;; TODO
  )

(defn run-tests [opts]
  ;; TODO
  )
