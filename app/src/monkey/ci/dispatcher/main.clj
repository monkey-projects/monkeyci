(ns monkey.ci.dispatcher.main
  "Main class for the dispatcher, used when running as a microservice"
  (:gen-class)
  (:require [monkey.ci.dispatcher.events :as de]))

(defn -main [& args]
  ;; TODO Start component system
  )
